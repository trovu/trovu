site_name: Trovu
site_description: Search with shortcuts.
site_url: https://trovu.net

docs_dir: docs
site_dir: dist/public/docs
theme:
  name: readthedocs

plugins:
  - search
  - minify:
      minify_html: true

# Navigation structure (if needed)
nav:
  - Home: index.md

extra:
  social:
    - type: github
      link: https://github.com/trovu/trovu/
